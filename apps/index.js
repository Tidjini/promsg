const { index } = require("./home");
const { Procom } = require("./procom");
const { Message } = require("./message");
const { Prosocket } = require("./common/Prosocket");

module.exports = {
  Procom,
  Message,
  Prosocket,
  index,
};
